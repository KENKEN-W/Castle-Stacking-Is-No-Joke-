# 五段重ね将軍 — デザインシステム v1

作成日：2026年9月19日  
対象仕様：[docs/spec-v1.md](docs/spec-v1.md)

本書はプロトタイプの見た目、画面構成、部品、演出を定める。ゲームルールは仕様書を優先する。今回はデザイン設計のみで、ゲームコードや画像素材は作成しない。

## 1. デザインコンセプト

### 「一手を考え、五段を築く。墨紺と金の城郭アーケード」

戦国の城郭を、昔のメダルゲームの筐体や得点表示に落とし込む。墨紺の落ち着いた盤面に、赤・青・緑の城が積み上がり、達成時に金が輝く。通常時は数字を比較しやすく、勝ち戦では明確に華やかさが増す。

プレイヤーが毎手見る順序は「現在の城 → 各列の最上段 → 配置できる列」。城の色もすぐに分かり、数字の安全な置き方と同色狙いの間で迷える構成にする。

| 設計原則 | 画面への反映 |
| --- | --- |
| 数字が主役 | 城の中央に太い算用数字。屋根や模様は数字の領域に重ねない |
| 和風は輪郭で表す | 屋根、石垣、額縁、縦長の旗。長い文章は読みやすい横書き |
| アーケードらしさはメリハリで表す | 金の縁、太い得点、押し込み感。常時点滅は使わない |
| プレイヤーの判断を残す | 置ける列はすべて同等に強調。おすすめの列や自動選択は設けない |
| 得点の扱いを正確に伝える | 盤面の見込み、戦累積、全体合計をラベルで区別する |
| 操作しやすさを優先する | 列全体がタップ対象。装飾を縮めても数字と操作領域は残す |

将軍のキャラクターや立体的な城の背景は初期版には不要。オリジナルの幾何学的な屋根と旗だけで世界観を成立させる。既存ゲームのロゴ、家紋、素材、音源は転用しない。

## 2. ブランドカラーの候補と推奨案

| 案 | 基本色 | 特徴 | このゲームとの相性 |
| --- | --- | --- | --- |
| A：墨紺 × 金（推奨） | 墨紺 `#141923`、金 `#EAC46A`、生成り `#F7F0DF` | 落ち着いた盤面と華やかな達成演出を両立 | 3色の城が目立ち、長く考える画面に向く |
| B：漆黒 × 朱 | 漆黒 `#1D1719`、朱 `#D94B3D`、淡金 `#E7CB8B` | 力強い戦国・祭りの印象 | 赤い城と主要操作の色が重なり、識別整理が必要 |
| C：古紙 × 藍 | 古紙 `#F0E6CE`、藍 `#233D59`、真鍮 `#9B711D` | 盤上遊戯や歴史絵巻に近い | 明るく親しみやすいが、暗い筐体という仕様からは離れる |

**Aを採用案とする。** 通常画面の目安は暗い背景・面が約75%、文字や境界が約15%、金と城の色が約10%。城が増えた際は城の面積を優先し、この比率に固執しない。

金はブランドと操作・達成の共通アクセント。赤・青・緑は城の属性として扱う。「赤い城だから危険」「緑の城だから置ける」という意味を持たせない。

## 3. カラーパレット

### 基本とUIの色

以下の名前を実装時の共通色設定名にする。部品ごとに似た色を増やさない。

| 設定名 | 色 | 用途 |
| --- | --- | --- |
| `color-bg` | `#141923` | ページ背景、盤面の奥 |
| `color-surface` | `#202938` | 得点パネル、ダイアログ、操作案内 |
| `color-surface-raised` | `#2B3648` | 浮き上がった小パネル、ホバーの面 |
| `color-border` | `#7D8BA0` | 操作対象の境界、空き段の輪郭 |
| `color-divider` | `#394556` | 装飾用の区切り。操作可否の唯一の手がかりにはしない |
| `color-text` | `#F7F0DF` | 見出し、本文、主要な数値 |
| `color-text-muted` | `#BBC3CF` | 補足、未確定ラベル、保存状態 |
| `color-gold` | `#EAC46A` | 主要ボタン、置ける列の枠、達成バッジ |
| `color-gold-light` | `#FFE3A0` | 金のハイライト。装飾に限定 |
| `color-ink` | `#17202B` | 金ボタンと城の色面に載せる数字・文字 |
| `color-caution` | `#FFC894` | 再合戦の得点破棄説明、配置エラーの印 |
| `color-focus` | `#F7F0DF` | キーボードフォーカスの外側リング |
| `color-overlay` | `#000000`、不透明度72% | ダイアログ背後の遮光 |

### 城の3色

| 属性 | 色面 | 数字・文字 | 色以外の識別 |
| --- | --- | --- | --- |
| 赤 | `#F28B82` | `#17202B` | 小さな丸印＋「赤」 |
| 青 | `#8ABEFF` | `#17202B` | 小さなひし形＋「青」 |
| 緑 | `#8FD5AC` | `#17202B` | 小さな三角印＋「緑」 |

城の3色は同じ形・同じ大きさで表示する。明るい色面を使い、小さい画面でも暗い盤面から分離する。丸・ひし形・三角は独自の単純な識別印で、追加の能力や役を意味しない。狭い城では形の印を省略しても、色名は残す。

### 配色の読みやすさ

指定した単色同士について、相対輝度によるコントラスト比を計算した。以下は設計値であり、実画面の検証完了を意味しない。

| 文字／背景 | 計算値（概数） |
| --- | --- |
| 本文／背景 | 15.49 : 1 |
| 本文／パネル | 12.87 : 1 |
| 補足文字／パネル | 8.23 : 1 |
| 金文字／パネル | 8.78 : 1 |
| 墨色文字／金ボタン | 9.86 : 1 |
| 墨色数字／赤い城 | 6.88 : 1 |
| 墨色数字／青い城 | 8.52 : 1 |
| 墨色数字／緑の城 | 9.61 : 1 |
| 注意文字／パネル | 9.71 : 1 |
| 境界／パネル | 4.23 : 1 |

本文は4.5 : 1以上、操作境界は3 : 1以上を設計上の目標とする。文字の背後をグラデーションにせず、城の数字・色名を半透明にしない。配置不能の列でも、配置済みの城の数字は薄くしない。

## 4. タイポグラフィ

外部Webフォントは使わず、端末内のフォントで成立させる。

| 用途 | 推奨フォント順 | 方針 |
| --- | --- | --- |
| ロゴ・大きい結果見出し | Yu Mincho、Hiragino Mincho ProN、serif | 太字。和風の存在感をここに集める |
| 本文・ボタン・説明 | system-ui、Yu Gothic、Hiragino Kaku Gothic ProN、Meiryo、sans-serif | 中程度～太字。文章は装飾しない |
| 城の数字・得点 | Arial、Helvetica、sans-serif | 太字、等幅数字指定。数字の桁が変わっても表示がぶれない |

| 要素 | スマートフォン | PC | 太さ／行間 |
| --- | --- | --- | --- |
| タイトル画面のロゴ | 36～48px | 56～72px | 700／1.15 |
| プレイ中のゲーム名 | 18～20px | 24px | 700／1.3 |
| 画面・ダイアログ見出し | 24px | 32px | 700／1.3 |
| 勝ち戦見出し | 40px | 64px | 700／1.15 |
| 城列内の数字 | 24px | 32px | 700～900／1 |
| 次に置く城の数字 | 40px | 48px | 700～900／1 |
| 最終得点 | 48px | 64px | 700～900／1.1 |
| 本文・主要ボタン | 16px | 16px | 400～700／1.6 |
| 得点ラベル・補足 | 14px | 14px | 500／1.5 |
| 城内の色名・列の短い状態表示 | 12px | 12～14px | 700／1.2 |

ロゴは「五段重ね」「将軍」の2行まで許容する。説明文に筆文字風の表現を使わない。数字は算用数字、戦番号は「第1戦」、城数は「14城」、段数は「5段」、得点は「75点」と書き分ける。

## 5. レイアウトと共通部品

### 余白・形

- 余白の基本単位は4px。4・8・12・16・24・32・48pxを使う。
- 通常パネルの角丸は8px、ボタンは6px、城は2px程度。筐体の硬さを残す。
- 境界は基本1px、配置可能な列は2px、フォーカスは外側3px＋間隔3px。
- 主要ボタンの高さは48px以上。短いアイコン操作も44×44px以上にする。
- パネルの影は下方向の控えめな影。強い発光は同色完成と勝ち戦だけに使う。
- 背景装飾は画面端の屋根影や細線程度。数字・説明の背後に模様を置かない。

### 画面幅

| 幅 | 方針 |
| --- | --- |
| 320～599px | 1カラム。画面左右12px、列間4px。得点を短い複数行に整理 |
| 600～899px | 1カラムを維持。左右24px、列間8px。盤面を中央配置 |
| 900px以上 | 全体最大960px、盤面最大720px。盤面の下に現在の城と補助情報 |

城列は全幅で常に5本横並び。幅320pxでは内側296pxから列間16pxを引き、各列56pxを確保する。列の枠や内側余白はこの56pxに含める。盤面を箱で何重にも囲んで実効幅を削らない。

縦方向は必要に応じてスクロールする。下部の現在の城を固定表示して盤面を覆う構成は採用しない。横向きの小さい画面も、文字の過度な縮小より縦スクロールを優先する。

### ボタン・通知

| 部品 | 見た目と動作 |
| --- | --- |
| 主要操作 | 金の塗り＋墨色文字。「はじめる」「つづきから」「同じ戦を続ける」など |
| 補助操作 | 暗い面＋明るい境界。「遊び方」「タイトルへ」 |
| 再合戦 | 暗い面＋注意色の境界、明確な得点破棄説明。主要操作と同じ大きさ |
| ホバー | 面や枠を少し明るくする。ホバーだけで情報を出さない |
| 押下 | 1～2px沈む短い変化。タップ直後に反応を伝える |
| フォーカス | 白い外側リング。金色の「置ける」枠とは別に表示 |
| 通知 | 盤面下の確保済み領域に表示。突然の高さ変化で城列を動かさない |

## 6. 主要画面のデザイン方針

### タイトル：小さな筐体の正面

中央に金のロゴ、その下にオリジナルの屋根シルエットを配置する。紹介は「数字を読み、色をそろえ、五段を築け。」程度の短い文にする。

「はじめる」「遊び方」を縦に配置。保存があれば主要ボタンは「つづきから」とし、「第2戦・現在の合計93点」のような要約を添える。「最初から遊ぶ」は補助操作にする。進行中の保存を消す際は、確認画面で「保存したゲームを破棄して始めます」と説明する。

保存された状態がゲーム終了なら「前回の結果を見る」と表示して結果を復元し、「もう一度遊ぶ」で新規ゲームを開始できるようにする。

### 遊び方：短い例から理解する

同じ暗いパネルに、次の順で小見出しと静止した例を置く。

1. 下から3→8→14と積む図。「上には大きい数字だけ。各列5城まで」
2. 14城＝0点、15城＝15点の比較。
3. 同色5段の例と倍率表。色を狙って15城に届かなければ盤面0点と明記。
4. 「戦終了」「再合戦」「勝ち戦」の違い。
5. タップ／クリック、キー1～5の操作。

再合戦の説明は「この戦の累積得点も捨てて0点から」。勝ち戦は「加点して同じ戦を続行」。上のルール図は例示であると分かる位置に置き、実際の未来の城と混同させない。

### ゲーム本編：判断のための盤面

画面の順序は以下を基本とする。

| 上からの順序 | 内容・強調 |
| --- | --- |
| 1 | 小さめのゲーム名、第何戦か、遊び方ボタン |
| 2 | 第1戦～第3戦の戦績。進行中は「挑戦中」、未来の戦は「未開始」、終了済みは点数 |
| 3 | 現在の合計スコア。内訳として「第N戦 累積○点」を表示 |
| 4 | 5本の城列。列番号、5段の城、置ける／満杯などの状態 |
| 5 | 大きい「次に置く城」と「配置可能：第1・3列」などの案内 |
| 6 | 積載18／25城、同色5段1列・×2、この盤面の得点見込み36点 |
| 7 | 短い操作説明、エラー通知、保存状態 |

14城以下では「この盤面の得点見込み 0点」「得点まであと○城」。同色列が完成していても加点演出をしない。15城到達時は「15城到達・得点条件達成」と短く表示し、未確定の得点を全体合計へ移動させない。

配置可能な列は金枠＋「置ける」。配置不可は「数字不足」または「満杯」と表示する。不可列も選択を受け取り、仕様どおり理由を通知する。見た目を薄くするために列全体を透明化しない。

列内に最適手の矢印を置かず、同色が続く列を自動で推奨しない。現在の城より先の城や未出現数字一覧も表示しない。

### 配置不能・再合戦の選択：得点を比較して決める

盤面が背後に残る中央ダイアログ。最大幅520px、狭い画面では左右16pxを空ける。見出しは「城をこれ以上積めません」。現在の城数、基本点、同色完成列数、倍率、盤面の獲得予定点を示す。

勝ち戦の累積70点＋現在盤面18点の例：

| 選択 | 表示する結果 |
| --- | --- |
| 戦終了 | 「この戦を88点で確定」 |
| 再合戦 | 「この戦の70点を破棄」「今回の18点も加算せず、0点から再開」 |

続けて「終了済みの戦の得点と、勝ち戦回数・最高積載数は残ります」と添える。2つのボタンを同じ大きさで縦に配置し、確定を金の塗り、再合戦を注意色の枠で区別する。再合戦を報酬付きの有利な操作に見せない。

ダイアログを開いた直後は見出しへフォーカスし、直前のキー入力で選択が確定しないようにする。背景操作・背景クリックによる閉じる操作は無効。選択肢と得点破棄説明が揃っているため、再合戦だけの追加確認ダイアログは設けない。

### 強制戦終了・戦結果：盤面0点と戦の得点を区別

「第1戦 終了」を生成り文字で表示。赤い全面表示や敗北を責める言葉は使わない。

14城で終了し、勝ち戦の累積が70点ある例では、「今回の盤面 14城・0点」「勝ち戦による累積70点」「第1戦 最終得点70点」を分ける。ボタンは「第2戦へ」。第3戦終了時はゲーム結果へ進む。

### 勝ち戦：ゲーム内で最も華やかな瞬間

勝ち戦条件を達成した盤面を残し、金の細線と小さな紙吹雪を盤面の外側中心に表示。「勝ち戦！」を大きく出し、「○城・同色5段○列」「基本点×倍率」「獲得○点」「第N戦 累積○点」を順に読めるようにする。

「第N戦は続きます」を添え、ボタンは「同じ戦を続ける」。自動で盤面を消さず、次の戦へ進んだと誤認させない。保存から復元した勝ち戦画面では、得点は既に加算済みとして表示する。

### ゲーム結果：戦績を持ち帰る画面

「五段重ね将軍」「戦績」、第1戦～第3戦の点数を整列。区切りの下に最大サイズの最終得点を置き、その直下に合計点に応じた評価を金枠で表示する。勝ち戦回数・再合戦回数・最高積載数は下の補助パネルへまとめる。

「もう一度遊ぶ」を主要ボタン、「タイトルへ」を補助ボタンにする。全国順位、最高得点更新表示は追加しない。

### 保存状態

通常は小さく「自動保存済み」。毎手の保存で通知を点滅させない。保存できない場合は「この環境では自動保存できません」を持続表示し、その場のプレイを妨げない。破損した保存はタイトルで説明し、復帰できない理由と新規開始の導線を示す。

## 7. 積み重ねる城のデザイン

### 1城は「屋根・色壁・石垣」の小さな駒

写真や精細な城の絵ではなく、正面から見た小さな城を1個の駒として作る。5個積んだとき、どこからどこまでが1城か分かることを優先する。

| 部位 | 形・役割 |
| --- | --- |
| 屋根 | 墨色の低い台形。両端をわずかに張り出し、薄い明るい縁を付ける |
| 壁 | 赤・青・緑の平らな色面。数字を中央に大きく配置 |
| 識別 | 壁の下端に色名と小さな形の印。数字と領域を分ける |
| 石垣 | 墨色の細い帯。2～3本の短い目地で城らしさを出す |

屋根・壁・石垣は通常のHTML要素とCSSの疑似要素や単純な図形で制作する想定。装飾は読み上げ対象にしない。画像生成・外部素材・アイコンライブラリは必須にしない。

### 寸法と積み方

| 項目 | スマートフォン | PC |
| --- | --- | --- |
| 1段の高さ | 約64px | 約80px |
| 駒本体の高さ | 約60px | 約74px |
| 屋根／壁／石垣の高さ目安 | 9／45／6px | 12／54／8px |
| 駒の幅 | 列内の利用可能幅すべて | 列内で最大108px、中央配置 |
| 次に置く城 | 幅96px、高さ100px程度 | 幅112px、高さ116px程度 |

5段分の領域を最初から確保し、一番下から埋める。空き段は細い水平線で示す。段が増えても数字を縮めない。駒同士の上下に4～6px程度の間を残し、屋根を次の城の数字に重ねない。

現在の最上段は、数字を隠さない細い生成りの上端線で示す。列外の金枠は配置可否、城の色面は属性、白い外側リングはフォーカスと、表示する場所を分ける。

### 城列の状態

| 状態 | 表現 |
| --- | --- |
| 空列・配置可能 | 5段の空き領域と金の列枠。「置ける」 |
| 配置可能 | 既存の城をそのまま表示し、列枠を金にする |
| 数字不足 | 通常の境界と「数字不足」。選択時に理由を下部通知 |
| 5段完成 | 「満杯」。城の数字と色はそのまま読める |
| 同色5段完成 | 列下部に金の小さな「同色5段」バッジ。完成時に一度だけ発光 |
| 不可列への入力 | 境界の短い注意表示＋文章。配置は動かさない |

狭い画面の「同色5段」は「同色」「5段」の2行でもよい。列の状態表示は高さを確保し、バッジの出現で盤面を上下に動かさない。同色完成のバッジ自体は、15城到達前でも表示するが、「得点獲得」とは表示しない。

## 8. 演出と入力の一貫性

| 場面 | 演出の目安 |
| --- | --- |
| 配置 | 120～180msでわずかに下がって着地。次の入力を長く待たせない |
| 不可列への入力 | 120ms程度の境界変化。揺らす場合も2px程度 |
| 同色5段 | 400～600msの金の発光を1回 |
| 15城到達 | 250ms程度のラベル切替 |
| 勝ち戦 | 800～1,200ms程度の紙吹雪と加点表示。ボタンを演出終了待ちにしない |
| 画面切替 | 150～200ms程度のフェード |

動きを減らす設定では移動・揺れ・紙吹雪・数値のカウントアップを省き、最終値と静止した印を直ちに表示する。装飾はクリックやタップを受け取らない。演出の完了を得点確定や保存の条件にしない。

キーボード1～5、Tab、Enter／Space、タップ、クリックは同じ結果を返す。城列内の各城を別々のボタンにせず、1列を1つの操作対象にする。読み上げには列番号、城数、最上段、配置可否、必要に応じて下からの数字と色を伝える。

## 9. 実装時のデザイン確認項目

- 320px幅でも5列が横並びで、2桁の数字、色名、操作範囲が欠けない。
- 375px幅、PC幅、横向きの低い画面、文字拡大時に、ダイアログとボタンへ到達できる。
- 3色を見分けにくい状態でも、色名から同色の列を判断できる。
- 配置不可や満杯でも数字の読みやすさが落ちない。
- 配置可能枠とキーボードフォーカスが同時に表示できる。
- 14城・同色完成列ありで「盤面0点」が明確。勝ち戦の累積点と混同しない。
- 再合戦の選択前に、失う戦累積得点と確定可能な得点の両方が分かる。
- 勝ち戦で「同じ戦を続ける」ことが伝わる。
- 保存から復帰しても、追加で加点したような表示にならない。
- 勝ち戦・再合戦後の参考記録の扱いが仕様と一致する。
- 通常時の装飾が数字を隠さず、演出中も操作対象を覆わない。
- 外部フォントや画像なしで成立し、OSごとのフォント差でボタンが欠けない。

現時点では配色の数値計算と仕様との照合のみを実施した。画面の表示確認、端末での操作確認、フォントの実寸確認はプロトタイプ実装後に行う。
